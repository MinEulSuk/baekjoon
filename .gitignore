# -------------------------------
# Visual Studio 핵심 무시 목록
# -------------------------------
# IntelliSense 및 로컬 설정 폴더 (숨김 폴더) - 용량이 크고 불필요함
.vs/

# 사용자별 로컬 설정 파일 (디버깅 경로 등 개인 설정)
*.user
*.suo
*.userosscache
*.sln.docstates

# -------------------------------
# 빌드 결과물 무시 (컴파일된 파일들)
# -------------------------------
# 64비트 빌드 폴더
x64/
# 32비트 빌드 폴더
Debug/
Release/
# 프로젝트 내부의 빌드 폴더 (경로에 따라 생길 수 있음)
*/x64/
*/Debug/
*/Release/

# -------------------------------
# 파일 확장자별 바이너리 무시
# -------------------------------
*.exe
*.dll
*.obj
*.pdb
*.ilk
*.aps
*.tlog
*.idb